{
  "private": true,
  "name": "doh-cache",
  "version": "1.0.0",
  "description": "A Cloudflare Worker that makes DNS-over-HTTPS (DoH) requests cacheable at edge.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fmt": "prettier --write '**/*.{js,ts,css,json,md}'",
    "deploy": "wrangler deploy"
  },
  "author": "milgradesec <38557680+milgradesec@users.noreply.github.com>",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "prettier": "^3.9.5",
    "typescript": "^7.0.2",
    "wrangler": "^4.112.0"
  }
}
